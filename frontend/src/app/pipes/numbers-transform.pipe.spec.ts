import { NumbersTransformPipe } from './numbers-transform.pipe';

describe('NumbersTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new NumbersTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
